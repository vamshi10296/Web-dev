const express = require("express");
const route=express.Router();

const studentController






route.get("/",studentController.home);
// route.get("/marks",studentController.marks);
// route.get("/attendence",studentController.home);
module.export=route;